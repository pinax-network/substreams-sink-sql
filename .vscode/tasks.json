{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "go build",
            "type": "shell",
            "command": "go build ${workspaceFolder}/cmd/substreams-sink-sql",
            "group": "build"
        }
    ]
}
